# ============================================================
# Direkte Abhängigkeiten – pip löst transitive automatisch auf
# ============================================================
# CUDA auf Windows: PyTorch bindet CUDA direkt ein.
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
torch>=2.11.0
torchaudio>=2.11.0

# Text-to-Speech (Coqui TTS - maintained fork idiap/coqui-ai-TTS)
# [codec] wird für PyTorch >= 2.9 benötigt (torchcodec)
coqui-tts[codec]

# Coqui TTS ist noch nicht mit transformers 5.x kompatibel
transformers<5.0.0

# Audio-Nachbearbeitung (WAV → MP3, ID3-Tags)
pydub
mutagen
